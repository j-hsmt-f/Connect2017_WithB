<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.connect2017withb.shareapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".DBActivity"
            android:label="@string/title_activity_db"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".KuchiActivity"
            android:label="@string/title_activity_db"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".MoreActivity"></activity>
        <activity android:name=".ShowDataBase" />
        <activity android:name=".ShowDataBaseK" />
        <activity android:name=".SightseeingActivity"></activity>
    </application>

</manifest>